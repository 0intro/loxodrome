<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background>
        <inset android:drawable="@mipmap/ic_launcher_background" android:inset="16.7%" />
    </background>
    <foreground>
        <inset android:drawable="@mipmap/ic_launcher_foreground" android:inset="16.7%" />
    </foreground>
    <!-- Themed icon (Android 13+). Not produced by @capacitor/assets, which
         rewrites this file: re-apply after any `assets generate` run. Same
         16.7% inset as the foreground, so the tinted mark lands exactly where
         the white one does. -->
    <monochrome>
        <inset android:drawable="@drawable/ic_launcher_monochrome" android:inset="16.7%" />
    </monochrome>
</adaptive-icon>