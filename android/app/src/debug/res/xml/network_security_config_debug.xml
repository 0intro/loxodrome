<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only (see the debug AndroidManifest.xml overlay):
     cleartext to loopback targets for `adb reverse` development, and
     nothing else; the base-config default (cleartext refused) stands
     for every other host. -->
<network-security-config>
	<domain-config cleartextTrafficPermitted="true">
		<domain includeSubdomains="false">localhost</domain>
		<domain includeSubdomains="false">127.0.0.1</domain>
		<domain includeSubdomains="false">10.0.2.2</domain>
	</domain-config>
</network-security-config>
