[Unit]
Description=Daily Loxodrome account-service backup

[Timer]
OnCalendar=daily
RandomizedDelaySec=1h
# A machine asleep at the appointed hour runs it at the next boot.
Persistent=true

[Install]
WantedBy=timers.target
